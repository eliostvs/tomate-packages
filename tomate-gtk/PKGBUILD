# Maintainer: Elio Esteves Duarte update desktop database ubuntu after install crea<elio.esteves.duarte@gmail.com>
pkgname=tomate-gtk
pkgver=0.0.0
pkgrel=1
pkgdesc="A timer for the Pomodoro Technique"
arch=('any')
url="https://github.com/eliostvs/tomate-gtk"
license=('GPL')
depends=('desktop-file-utils'
         'gtk-update-icon-cache'
         'gtk3'
         'hicolor-icon-theme'
         'python'
         'python-tomate')
optdependes('tomate-alarm-plugin: Plugin that plays a alarm at session end'
            'tomate-indicator-plugin: Plugin that shows the session progress in the notification area (indicator)'
            'tomate-notify-plugin: Plugin that shows screen notifications'
            'tomate-exec-plugin: Run commands when timer starts, stops or finishes'
            'tomate-statusicon-plugin: Plugin that shows the session progress in the notification area')
makedepends=('python-setuptools')
options=(!emptydirs)
source=("https://github.com/eliostvs/$pkgname/archive/$pkgver.tar.gz")
md5sums=('')
install=tomate-gtk.install

package() {
  cd "$srcdir/$pkgname-$pkgver"
  python setup.py install --root="$pkgdir/" --optimize=1
  install -D -m644 COPYING "${pkgdir}/usr/share/licenses/${pkgbase}/LICENSE"
}

# vim:set ts=2 sw=2 et:
