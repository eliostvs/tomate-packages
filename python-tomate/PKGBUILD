# Maintainer: Elio Esteves Duarte <elio.esteves.duarte@gmail.com>
_pkgname=tomate
pkgname=python-$_pkgname
pkgver=0.0.0
pkgrel=1
pkgdesc=("This package has been joined to tomate-gtk.
         "To prevent upgrade problems in the tomate-gtk, this package will be still a dependency of it but will be empty.")
arch=("any")
url="https://github.com/eliostvs/tomate/"
license=("GPL")
options=(!emptydirs)
source=("https://github.com/eliostvs/$_pkgname/archive/$pkgver.tar.gz")

package() {
}

# vim:set ts=2 sw=2 et:
